backend:
  name: git-gateway
  branch: main

media_folder: "assets"
public_folder: "/assets"

collections:
  - name: "site"
    label: "Site"
    files:
      - file: "index.html"
        label: "Home"
        name: "home"
        fields:
          - label: "Body"
            name: "body"
            widget: "text"